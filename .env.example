# =================================================================
# Environment Configuration Example
# Copy this file to .env.dev and fill in actual values.
# =================================================================

# -----------------------------------------------------------------
# 1. Core Database Configuration (Required for all environments)
# -----------------------------------------------------------------
# The JDBC URL for the PostgreSQL database connection
DB_URL="jdbc:postgresql://<host>:<port>/<database_name>"
# Database username
DB_USERNAME="<your_db_username>"
# Database password
DB_PASSWORD="<your_db_password>"

# -----------------------------------------------------------------
# 2. Spring Boot & Application Settings (Development Specific)
# -----------------------------------------------------------------
# Specifies the active Spring profiles (e.g., dev, prod, test).
SPRING_PROFILES_ACTIVE=dev

# Application server port
SERVER_PORT=8080

# Sets the secure flag for the session cookie. Use 'true' in HTTPS/production.
COOKIE_SECURE=false

# Comma-separated list of allowed origins for CORS.
CORS_ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"

# Boolean flag to enable the DataLoader component to recreate/load initial data.
# Set to 'true' to load data, 'false' to skip.
DEV_DATA_LOADER_ENABLED=false
SHOULD_CREATE_INITIAL_USERS=false